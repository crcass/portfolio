import styled from 'styled-components';

const Navbar = styled.nav`
  background: #fff;
  height: fit-content;
  left: 0;
  position: fixed;
  top: 0;
  width: 100%;
  z-index: 100;
`;

export default Navbar;
