import styled from 'styled-components';

const SiteContainer = styled.main({
  display: 'flex',
  flex: '1 0 auto',
  margin: '97px 44px 90px',
  maxWidth: '1440px',
});

export default SiteContainer;
